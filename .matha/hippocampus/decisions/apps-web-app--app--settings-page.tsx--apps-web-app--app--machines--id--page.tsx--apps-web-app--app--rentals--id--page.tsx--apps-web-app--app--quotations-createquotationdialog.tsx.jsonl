{"id":"1789484313535-e05a918e576ddb1e","timestamp":"2026-09-15T14:58:33.535Z","component":"apps/web/app/(app)/settings/page.tsx, apps/web/app/(app)/machines/[id]/page.tsx, apps/web/app/(app)/rentals/[id]/page.tsx, apps/web/app/(app)/quotations/CreateQuotationDialog.tsx","previous_assumption":"assumed the useState-from-searchParams stale-initializer bug found on Quotations/Billing was isolated to those two files, and assumed a changed [id] dynamic route segment forces a page remount the same way a genuinely different route does","correction":"Confirmed via full sweep: the App Router remounts a page for NEITHER a query-only navigation NOR a changed [id] segment on the same route (verified no key={id} anywhere in the tree, no template.tsx files exist) -- so this bug class also hits cross-record navigation (opening a notification/search hit for a different machine or rental while already viewing one), not just query-only changes. Found and fixed three more real instances: settings/page.tsx's tab state (sidebar Organization/Settings links do nothing while already on Settings in a different tab), machines/[id] and rentals/[id]'s tab state (switching to a different id's detail page keeps showing whatever tab was selected for the previous record instead of resetting), and CreateQuotationDialog's loadingContext/loadingAuctionPrefill (the dialog stays mounted, only xdg-open - opens a file or URL in the user's preferred application\n\nSynopsis\n\nxdg-open { file | URL }\n\nxdg-open { --help | --manual | --version }\n\nUse 'man xdg-open' or 'xdg-open --manual' for additional info. toggles, so a second notification for a different requirement after these flags already settled to false never flips them back to true -- form briefly renders as a manual quotation, no locked renter/pre-filled rate, for the duration of the re-fetch). Also confirmed NOT live bugs and left alone: auctions/page.tsx's selectedRequirementId (every path to it crosses a different route first), transport/[id]/page.tsx's rentalId (self-healing, the loading effect already unconditionally re-sets it), CreateQuotationDialog's customerMode (masked by isFromRequirement, itself a correctly-recomputed plain const). Separately, while wiring a Requested-quotations feature that cross-references CommercialQuotation.quotationResponseId, discovered CreateQuotationDialog never actually sent quotationResponseId when creating from a requirement at all -- the field exists on the contract and the backend already validates it (see the Path A test in commercial-quotation-service.test.ts), the frontend just never populated it, meaning every quotation ever formalized from a response was silently unlinked -- likely also why requirements/[id]/page.tsx's Open-{reference} link next to a response has never resolved. Fixed by having the dialog also fetch the Rental Company's own response for the requirement (best-effort) and include its id in the create payload.","trigger":"mcp-call","confidence":"probable","status":"active","supersedes":null,"session_id":"1789484313535-e05a918e576ddb1e"}
