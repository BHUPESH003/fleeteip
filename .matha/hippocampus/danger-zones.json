{
  "zones": [
    {
      "id": "danger-1788976353038-10e806e1c54ee447",
      "component": "apps/api/src/infrastructure/database/client.ts, apps/api/src/infrastructure/database/types.ts",
      "pattern": "Any new Postgres `numeric` column will come back from `pg`/Kysely as a JS string, not a number, unless a type parser is registered for OID 1700. Already hit and fixed once for `products.capacity` via `types.setTypeParser(1700, parseFloat)` in client.ts. If a future numeric column doesn't behave as a real `number` at runtime despite the Kysely type saying `number`, check this type-parser registration first before assuming a repository or contract bug.",
      "description": "Any new Postgres `numeric` column will come back from `pg`/Kysely as a JS string, not a number, unless a type parser is registered for OID 1700. Already hit and fixed once for `products.capacity` via `types.setTypeParser(1700, parseFloat)` in client.ts. If a future numeric column doesn't behave as a real `number` at runtime despite the Kysely type saying `number`, check this type-parser registration first before assuming a repository or contract bug.",
      "confidence": "probable"
    },
    {
      "id": "danger-1788976355652-1470e8384e67a059",
      "component": "apps/api/src/modules/*/domain/ports.ts",
      "pattern": "Repository port interfaces and record types must live in their OWN module's domain/ports.ts, never get appended to a neighboring module's ports file for convenience. This actually happened once: Machine's port/record ended up inside modules/catalogue/domain/ports.ts during initial Step 4 work and had to be moved into modules/equipment/domain/ports.ts. It directly undermines the reason catalogue and equipment were split into two modules (catalogue must stay ownerless/platform-level with no knowledge of org-owned entities). When reviewing a new port, check the file path it landed in, not just whether the interface content is correct.",
      "description": "Repository port interfaces and record types must live in their OWN module's domain/ports.ts, never get appended to a neighboring module's ports file for convenience. This actually happened once: Machine's port/record ended up inside modules/catalogue/domain/ports.ts during initial Step 4 work and had to be moved into modules/equipment/domain/ports.ts. It directly undermines the reason catalogue and equipment were split into two modules (catalogue must stay ownerless/platform-level with no knowledge of org-owned entities). When reviewing a new port, check the file path it landed in, not just whether the interface content is correct.",
      "confidence": "probable"
    },
    {
      "id": "danger-1788976361417-3fdfe6ca9da3f2d2",
      "component": "apps/api/src/app.ts",
      "pattern": "Fastify 5's `setErrorHandler` defaults its error-type generic to `unknown` unless explicitly set (e.g. `setErrorHandler<FastifyError>(...)`, importing `FastifyError` from \"fastify\"). A plain `(error, request, reply) => {...}` handler that accesses `error.statusCode`/`error.code` outside an `instanceof AppError` branch will fail `tsc --noEmit` with TS18046. This was introduced during an earlier logout-bug fix and went unnoticed for multiple sessions because typecheck verification was repeatedly scoped to `packages/contracts` only, never the full `apps/api` package. Always run `pnpm --filter @fleetip/api run typecheck` (not just the contracts one) before calling API-side work verified.",
      "description": "Fastify 5's `setErrorHandler` defaults its error-type generic to `unknown` unless explicitly set (e.g. `setErrorHandler<FastifyError>(...)`, importing `FastifyError` from \"fastify\"). A plain `(error, request, reply) => {...}` handler that accesses `error.statusCode`/`error.code` outside an `instanceof AppError` branch will fail `tsc --noEmit` with TS18046. This was introduced during an earlier logout-bug fix and went unnoticed for multiple sessions because typecheck verification was repeatedly scoped to `packages/contracts` only, never the full `apps/api` package. Always run `pnpm --filter @fleetip/api run typecheck` (not just the contracts one) before calling API-side work verified.",
      "confidence": "probable"
    },
    {
      "id": "danger-1788976755153-151fc2edd0dd9c30",
      "component": "apps/api/src/modules/catalogue/application/catalogue-service.ts, apps/api/src/modules/equipment/application/equipment-service.ts",
      "pattern": "An application service returning a repository Record directly (snake_case: created_at, organization_id, product_category_id, ...) instead of mapping it to the matching camelCase contract type from packages/contracts ships a response that silently doesn't match its own Zod schema. This actually shipped once: CatalogueService/EquipmentService both returned raw *Record objects straight from the repository, and it was only caught by an actual HTTP round-trip test (curl against a running dev server), not by typecheck/lint/unit tests — none of those layers verify the HTTP response body's real shape. The existing correct precedent was already in the codebase (AuthService.getAuthenticatedSession manually maps membership rows to camelCase) but wasn't followed when the two new services were written. Fix pattern: a small `toX(record): ContractType` mapper per entity, with the service method's return type explicitly annotated as the imported contract type (e.g. `Promise<Product[]>`) so a missing/misnamed field becomes a compile error next time instead of a silent runtime shape mismatch. When adding a new service, always do one real HTTP call against it, don't rely on `tsc`/tests alone to prove the response shape is right.",
      "description": "An application service returning a repository Record directly (snake_case: created_at, organization_id, product_category_id, ...) instead of mapping it to the matching camelCase contract type from packages/contracts ships a response that silently doesn't match its own Zod schema. This actually shipped once: CatalogueService/EquipmentService both returned raw *Record objects straight from the repository, and it was only caught by an actual HTTP round-trip test (curl against a running dev server), not by typecheck/lint/unit tests — none of those layers verify the HTTP response body's real shape. The existing correct precedent was already in the codebase (AuthService.getAuthenticatedSession manually maps membership rows to camelCase) but wasn't followed when the two new services were written. Fix pattern: a small `toX(record): ContractType` mapper per entity, with the service method's return type explicitly annotated as the imported contract type (e.g. `Promise<Product[]>`) so a missing/misnamed field becomes a compile error next time instead of a silent runtime shape mismatch. When adding a new service, always do one real HTTP call against it, don't rely on `tsc`/tests alone to prove the response shape is right.",
      "confidence": "probable"
    },
    {
      "id": "danger-1789005371337-5e3ff266bc350987",
      "component": "apps/web/app/machines/page.tsx",
      "pattern": "`legalNextStatuses()` in this file is a deliberate, small duplication of the server's `canTransition()` state-machine rule (apps/api/src/modules/equipment/domain/machine-status.ts), used only to decide which status-change buttons the UI offers. Do not \"fix\" this duplication by importing server domain logic into the browser bundle, and do not assume the UI rule is the source of truth — `canTransition` on the server remains the sole enforcement point and must be updated first if the state machine ever changes; this client copy just needs to be kept in sync manually.",
      "description": "`legalNextStatuses()` in this file is a deliberate, small duplication of the server's `canTransition()` state-machine rule (apps/api/src/modules/equipment/domain/machine-status.ts), used only to decide which status-change buttons the UI offers. Do not \"fix\" this duplication by importing server domain logic into the browser bundle, and do not assume the UI rule is the source of truth — `canTransition` on the server remains the sole enforcement point and must be updated first if the state machine ever changes; this client copy just needs to be kept in sync manually.",
      "confidence": "probable"
    },
    {
      "id": "danger-1789006262148-1ed8cf4932b53b21",
      "component": "apps/web/app/machines/page.tsx",
      "pattern": "In an async React event handler, never read `event.currentTarget` after an `await` — React nulls it out once the synchronous part of the event dispatch finishes, so `event.currentTarget.reset()` (or any other access) throws \"Cannot read properties of null\" post-await. Capture the element into a local variable (e.g. `const formElement = event.currentTarget`) before the first `await`, then use that local everywhere after. In `handleRegister` this bug also silently broke the follow-up `refreshMachines()` call: the throw happened on the line right before it, so newly-created machines never appeared in the list even though the POST had actually succeeded (surfaced as a generic \"Cannot read properties of null\" error).",
      "description": "In an async React event handler, never read `event.currentTarget` after an `await` — React nulls it out once the synchronous part of the event dispatch finishes, so `event.currentTarget.reset()` (or any other access) throws \"Cannot read properties of null\" post-await. Capture the element into a local variable (e.g. `const formElement = event.currentTarget`) before the first `await`, then use that local everywhere after. In `handleRegister` this bug also silently broke the follow-up `refreshMachines()` call: the throw happened on the line right before it, so newly-created machines never appeared in the list even though the POST had actually succeeded (surfaced as a generic \"Cannot read properties of null\" error).",
      "confidence": "probable"
    },
    {
      "id": "danger-1789021680104-e854329e41682947",
      "component": "apps/web/app/globals.css, packages/ui/src",
      "pattern": "Tailwind v4's automatic content scan (in `apps/web`, whose only CSS entry is `apps/web/app/globals.css`) does not reach into `packages/ui/src` on its own, even though Next's `transpilePackages` makes that package's TSX source get bundled into the app's JS. Any utility class used ONLY inside a `@fleetip/ui` component (and nowhere in `apps/web`'s own `.tsx` source) is silently dropped from the compiled CSS — no error, no warning, the class just never renders. Caught this by grepping the served `/_next/static/css/app/layout.css` for classes unique to `EmptyState`/`LoadingState`/`ErrorState` (`border-dashed`, `animate-spin`, `border-red-200`) and finding them absent, while classes that happened to also appear in an `apps/web` file (e.g. `shadow-sm`, `rounded-full`) were present and gave a false sense that scanning was working. Fixed with an explicit `@source \"../../../packages/ui/src\";` line in `globals.css`, right after `@import \"tailwindcss\"`. Rule of thumb: any time a Tailwind-styled component lives in a workspace package outside the consuming app's own directory tree, verify its classes actually made it into the compiled CSS by grepping the served stylesheet for a class unique to that component — don't assume Tailwind's content scan crosses workspace-package boundaries on its own.",
      "description": "Tailwind v4's automatic content scan (in `apps/web`, whose only CSS entry is `apps/web/app/globals.css`) does not reach into `packages/ui/src` on its own, even though Next's `transpilePackages` makes that package's TSX source get bundled into the app's JS. Any utility class used ONLY inside a `@fleetip/ui` component (and nowhere in `apps/web`'s own `.tsx` source) is silently dropped from the compiled CSS — no error, no warning, the class just never renders. Caught this by grepping the served `/_next/static/css/app/layout.css` for classes unique to `EmptyState`/`LoadingState`/`ErrorState` (`border-dashed`, `animate-spin`, `border-red-200`) and finding them absent, while classes that happened to also appear in an `apps/web` file (e.g. `shadow-sm`, `rounded-full`) were present and gave a false sense that scanning was working. Fixed with an explicit `@source \"../../../packages/ui/src\";` line in `globals.css`, right after `@import \"tailwindcss\"`. Rule of thumb: any time a Tailwind-styled component lives in a workspace package outside the consuming app's own directory tree, verify its classes actually made it into the compiled CSS by grepping the served stylesheet for a class unique to that component — don't assume Tailwind's content scan crosses workspace-package boundaries on its own.",
      "confidence": "probable"
    },
    {
      "id": "danger-1789037409571-753920d7422994fd",
      "component": "packages/contracts/src/organization/index.ts, infrastructure/database/migrations/0007_seed_rental_permission.ts",
      "pattern": "Adding a new permission code to `permissionCodeSchema` + `PERMISSION_ORGANIZATION_TYPES` (the contracts-level declaration) does NOT make that permission real in the database — the actual `permissions` table row and the `role_permissions` grant to `owner` are a separate, easy-to-forget step. Confirmed live: `rental.manage` was added to both contracts constructs in Step 2, but every single Rental E2E request (even from a legitimate Rental Company owner) 403'd until a dedicated seed migration (mirroring 0005's `equipment.manage` grant exactly) actually inserted the `permissions` row and the `role_permissions` link. `PERMISSION_ORGANIZATION_TYPES`'s `Record<PermissionCode, ...>` completeness check (Step 1's safety net) catches a missing *organization-type* entry at compile time, but has no way to catch a missing *database seed* — that gap is invisible to `tsc`, only shows up as a live 403. Rule for the next new permission: after adding it to the contracts enum + type map, always add the matching `permissions`/`role_permissions` seed migration in the same pass, and verify with one live request before considering the permission \"done\" — don't rely on typecheck passing as proof it works.",
      "description": "Adding a new permission code to `permissionCodeSchema` + `PERMISSION_ORGANIZATION_TYPES` (the contracts-level declaration) does NOT make that permission real in the database — the actual `permissions` table row and the `role_permissions` grant to `owner` are a separate, easy-to-forget step. Confirmed live: `rental.manage` was added to both contracts constructs in Step 2, but every single Rental E2E request (even from a legitimate Rental Company owner) 403'd until a dedicated seed migration (mirroring 0005's `equipment.manage` grant exactly) actually inserted the `permissions` row and the `role_permissions` link. `PERMISSION_ORGANIZATION_TYPES`'s `Record<PermissionCode, ...>` completeness check (Step 1's safety net) catches a missing *organization-type* entry at compile time, but has no way to catch a missing *database seed* — that gap is invisible to `tsc`, only shows up as a live 403. Rule for the next new permission: after adding it to the contracts enum + type map, always add the matching `permissions`/`role_permissions` seed migration in the same pass, and verify with one live request before considering the permission \"done\" — don't rely on typecheck passing as proof it works.",
      "confidence": "probable"
    },
    {
      "id": "danger-1789097649300-a512533a5a567d4a",
      "component": "apps/api/src/modules/identity/application/auth-service.ts",
      "pattern": "When a new PermissionCode is added to PERMISSION_ORGANIZATION_TYPES, AuthService.getAuthenticatedSession's per-membership `permissions` array (returned by /auth/me) must filter each role-granted code through PERMISSION_ORGANIZATION_TYPES[code].includes(organizationTypeCode) — it previously just returned every permission the role grants (roleRepository.listPermissionCodesByRoleId), unfiltered by the membership's actual organization type. Caught live during Auction/RFQ E2E testing: a Renter's own /auth/me response listed rental_company-only codes (equipment.manage, rental.manage, auction.participate, quotation.manage) it could never actually exercise (PermissionService.hasPermission still correctly denied them — this was a display/nav-menu correctness bug, not an authorization hole). Fixed by filtering the list against PERMISSION_ORGANIZATION_TYPES using row.organization_type_code before returning it. Any future change to PERMISSION_ORGANIZATION_TYPES or to how memberships are listed should re-verify this filter is still applied.",
      "description": "When a new PermissionCode is added to PERMISSION_ORGANIZATION_TYPES, AuthService.getAuthenticatedSession's per-membership `permissions` array (returned by /auth/me) must filter each role-granted code through PERMISSION_ORGANIZATION_TYPES[code].includes(organizationTypeCode) — it previously just returned every permission the role grants (roleRepository.listPermissionCodesByRoleId), unfiltered by the membership's actual organization type. Caught live during Auction/RFQ E2E testing: a Renter's own /auth/me response listed rental_company-only codes (equipment.manage, rental.manage, auction.participate, quotation.manage) it could never actually exercise (PermissionService.hasPermission still correctly denied them — this was a display/nav-menu correctness bug, not an authorization hole). Fixed by filtering the list against PERMISSION_ORGANIZATION_TYPES using row.organization_type_code before returning it. Any future change to PERMISSION_ORGANIZATION_TYPES or to how memberships are listed should re-verify this filter is still applied.",
      "confidence": "probable"
    },
    {
      "id": "danger-1789101794016-26d718360fd75440",
      "component": "apps/api/src/modules/identity/application/auth-service.ts",
      "pattern": "AuthService.login short-circuits on `!user || !(await verifyPassword(...))` — when the email doesn't exist, verifyPassword (a slow scrypt KDF) is never invoked, so the rejection is near-instant; when the email exists but the password is wrong, the response is delayed by a full scrypt computation. This response-time difference lets an attacker enumerate valid account emails via timing (CWE-208/204) even though the error message itself (\"Invalid email or password\") is identical either way. Full security audit (2026-09-11) found no other authz/SQLi/session issues — every application-service method checked correctly verifies resource ownership after PermissionService.requirePermission, and all `sql\\`...\\`` usages are properly parameterized. Fix pattern if addressed later: always run a dummy scrypt hash (e.g. against a fixed/precomputed hash) on the not-found path so both branches take comparable time.",
      "description": "AuthService.login short-circuits on `!user || !(await verifyPassword(...))` — when the email doesn't exist, verifyPassword (a slow scrypt KDF) is never invoked, so the rejection is near-instant; when the email exists but the password is wrong, the response is delayed by a full scrypt computation. This response-time difference lets an attacker enumerate valid account emails via timing (CWE-208/204) even though the error message itself (\"Invalid email or password\") is identical either way. Full security audit (2026-09-11) found no other authz/SQLi/session issues — every application-service method checked correctly verifies resource ownership after PermissionService.requirePermission, and all `sql\\`...\\`` usages are properly parameterized. Fix pattern if addressed later: always run a dummy scrypt hash (e.g. against a fixed/precomputed hash) on the not-found path so both branches take comparable time.",
      "confidence": "probable"
    },
    {
      "id": "danger-1789102370477-8bac7645589470a0",
      "component": "apps/api/build.mjs",
      "pattern": "Every real npm dependency added to apps/api must be added to build.mjs's esbuild `external` array, or the production build (`pnpm --filter @fleetip/api run build`) will silently produce a broken bundle that only fails at runtime, not at typecheck/lint/test time. Caught live: adding @fastify/rate-limit (for the auth rate-limiting security fix) broke the bundled dist/index.js with \"Dynamic require of 'node:net' is not supported\" — the package's internal CJS/ESM interop doesn't survive esbuild bundling. tsx (used for `pnpm dev`) never bundles anything, so this class of bug is invisible until someone actually runs the production build+start path, which nothing in CI/typecheck/unit-tests exercises. Fixed by adding \"@fastify/rate-limit\" to the external list. Rule: whenever a new dependency is added to apps/api/package.json, run `pnpm --filter @fleetip/api run build && node dist/index.js` (against any reachable Postgres) as a real smoke test before considering the change done — this is the only thing that actually exercises the esbuild bundling path.",
      "description": "Every real npm dependency added to apps/api must be added to build.mjs's esbuild `external` array, or the production build (`pnpm --filter @fleetip/api run build`) will silently produce a broken bundle that only fails at runtime, not at typecheck/lint/test time. Caught live: adding @fastify/rate-limit (for the auth rate-limiting security fix) broke the bundled dist/index.js with \"Dynamic require of 'node:net' is not supported\" — the package's internal CJS/ESM interop doesn't survive esbuild bundling. tsx (used for `pnpm dev`) never bundles anything, so this class of bug is invisible until someone actually runs the production build+start path, which nothing in CI/typecheck/unit-tests exercises. Fixed by adding \"@fastify/rate-limit\" to the external list. Rule: whenever a new dependency is added to apps/api/package.json, run `pnpm --filter @fleetip/api run build && node dist/index.js` (against any reachable Postgres) as a real smoke test before considering the change done — this is the only thing that actually exercises the esbuild bundling path.",
      "confidence": "probable"
    },
    {
      "id": "danger-1789108759118-85734d8a2eeed676",
      "component": "apps/web",
      "pattern": "Running `pnpm --filter @fleetip/web run build` (production `next build`) while a `next dev` process is already running against the same `apps/web/.next` directory corrupts that directory for the live dev server: the running process keeps stale in-memory webpack-runtime module references (e.g. \"Cannot find module './644.js'\") and starts 500ing on every route, even after deleting `.next` — deleting the folder does not let the live process self-heal. The only fix is to kill and restart the `next dev` process (its own `next-server` child too) so it recompiles fresh; the API's `tsx watch` process is unaffected by this (different build step). When verifying a production build during active dev-server work, either stop `next dev` first or run the production build against a separate checkout/worktree.",
      "description": "Running `pnpm --filter @fleetip/web run build` (production `next build`) while a `next dev` process is already running against the same `apps/web/.next` directory corrupts that directory for the live dev server: the running process keeps stale in-memory webpack-runtime module references (e.g. \"Cannot find module './644.js'\") and starts 500ing on every route, even after deleting `.next` — deleting the folder does not let the live process self-heal. The only fix is to kill and restart the `next dev` process (its own `next-server` child too) so it recompiles fresh; the API's `tsx watch` process is unaffected by this (different build step). When verifying a production build during active dev-server work, either stop `next dev` first or run the production build against a separate checkout/worktree.",
      "confidence": "probable"
    },
    {
      "id": "danger-1789109968723-786765a5cce200e2",
      "component": "apps/web/app/(app)",
      "pattern": "A form's onSubmit handler must never read `event.currentTarget` after an `await` — React nulls out the SyntheticEvent's `currentTarget` once the synchronous handler invocation returns, so any access after the first `await` (e.g. `event.currentTarget.reset()` in a success branch) throws \"Cannot read properties of null (reading 'reset')\" at runtime, even though it typechecks fine. Found live in `QuotationRow.handleOffer` (quotations/page.tsx) and `InvoiceCard.handlePayment` (billing/page.tsx) — both fixed by capturing `const formElement = event.currentTarget` synchronously, before the `await`, and using that local afterward. This is the same pattern already used correctly elsewhere in the app (e.g. `CreateQuotationForm.handleCreate`) — when adding a new async form handler, always capture `event.currentTarget` into a local first if anything after the first `await` needs it.",
      "description": "A form's onSubmit handler must never read `event.currentTarget` after an `await` — React nulls out the SyntheticEvent's `currentTarget` once the synchronous handler invocation returns, so any access after the first `await` (e.g. `event.currentTarget.reset()` in a success branch) throws \"Cannot read properties of null (reading 'reset')\" at runtime, even though it typechecks fine. Found live in `QuotationRow.handleOffer` (quotations/page.tsx) and `InvoiceCard.handlePayment` (billing/page.tsx) — both fixed by capturing `const formElement = event.currentTarget` synchronously, before the `await`, and using that local afterward. This is the same pattern already used correctly elsewhere in the app (e.g. `CreateQuotationForm.handleCreate`) — when adding a new async form handler, always capture `event.currentTarget` into a local first if anything after the first `await` needs it.",
      "confidence": "probable"
    },
    {
      "id": "danger-1789120126547-3d270a6c9fd78da8",
      "component": "apps/web/components/Header.tsx",
      "pattern": "A flex container using `justify-between` with a sibling that has a responsive `display:none` (e.g. `sm:hidden` on a mobile-only hamburger button) will NOT push the remaining visible child to the far edge on the breakpoint where the hidden sibling disappears — `justify-content: space-between` only distributes space \"between\" items, and with exactly one remaining flex item there is no second item to space against, so it just sits at flex-start. Found live via a screenshot review: adding `<NotificationBell />` as the header's second child (mobile hamburger being the first, `sm:hidden`) rendered the bell pinned to the LEFT edge of the header on desktop, not the right — and its `absolute right-0` dropdown panel then rendered mostly off-screen/behind the sidebar. Fixed by wrapping the always-visible right-side content (bell + account menu) in its own `<div className=\"ml-auto flex items-center gap-4\">` so it's pushed right regardless of whether the mobile-only sibling is present in layout. When adding a new always-visible element next to a responsively-hidden one in a `justify-between` flex row, don't trust the visual result without actually looking at a real screenshot at the breakpoint where the sibling disappears.",
      "description": "A flex container using `justify-between` with a sibling that has a responsive `display:none` (e.g. `sm:hidden` on a mobile-only hamburger button) will NOT push the remaining visible child to the far edge on the breakpoint where the hidden sibling disappears — `justify-content: space-between` only distributes space \"between\" items, and with exactly one remaining flex item there is no second item to space against, so it just sits at flex-start. Found live via a screenshot review: adding `<NotificationBell />` as the header's second child (mobile hamburger being the first, `sm:hidden`) rendered the bell pinned to the LEFT edge of the header on desktop, not the right — and its `absolute right-0` dropdown panel then rendered mostly off-screen/behind the sidebar. Fixed by wrapping the always-visible right-side content (bell + account menu) in its own `<div className=\"ml-auto flex items-center gap-4\">` so it's pushed right regardless of whether the mobile-only sibling is present in layout. When adding a new always-visible element next to a responsively-hidden one in a `justify-between` flex row, don't trust the visual result without actually looking at a real screenshot at the breakpoint where the sibling disappears.",
      "confidence": "probable"
    },
    {
      "id": "danger-1789128226985-50ed762f2fef896e",
      "component": "apps/web/app/(app)/auctions/page.tsx",
      "pattern": "Any polling load() function (useInterval-driven) that resets its main display state to null at the top of the function — before the new fetch resolves — will flicker on every poll tick: the component briefly renders its \"empty/not-found\" branch (EmptyState, join-button, \"auction ended\" text) and unmounts the live form (e.g. the bid input/button) before the fresh data lands ~a network round-trip later. This was the root cause of a reported flicker in RentalCompanyAuctionPanel (setAuction(null); setDetail(null) at the top of every load(), including poll-triggered calls, not just the initial mount). Fix pattern: only clear state on a genuine terminal outcome (404 \"no longer exists\" / real error), and let a successful fetch replace state in place without an intermediate null. Verified live: sampling the DOM every 250ms for >2 poll cycles (9s at a 4s interval) with the fix applied showed the bid form staying continuously mounted.",
      "description": "Any polling load() function (useInterval-driven) that resets its main display state to null at the top of the function — before the new fetch resolves — will flicker on every poll tick: the component briefly renders its \"empty/not-found\" branch (EmptyState, join-button, \"auction ended\" text) and unmounts the live form (e.g. the bid input/button) before the fresh data lands ~a network round-trip later. This was the root cause of a reported flicker in RentalCompanyAuctionPanel (setAuction(null); setDetail(null) at the top of every load(), including poll-triggered calls, not just the initial mount). Fix pattern: only clear state on a genuine terminal outcome (404 \"no longer exists\" / real error), and let a successful fetch replace state in place without an intermediate null. Verified live: sampling the DOM every 250ms for >2 poll cycles (9s at a 4s interval) with the fix applied showed the bid form staying continuously mounted.",
      "confidence": "probable"
    },
    {
      "id": "danger-1789394380139-ff0f65c894fe3d75",
      "component": "matha decision recording (component field)",
      "pattern": "matha's recordDecision derives the per-component .jsonl filename directly from the 'component' string (path-joined, slashes replaced) — a long, multi-file component string (e.g. 5 file paths joined with commas) can exceed the filesystem's max filename length and fail with ENAMETOOLONG, silently aborting that one record. Keep 'component' to 1-2 short paths or a short label; put the full list of touched files in the correction/previous_assumption text instead.",
      "description": "matha's recordDecision derives the per-component .jsonl filename directly from the 'component' string (path-joined, slashes replaced) — a long, multi-file component string (e.g. 5 file paths joined with commas) can exceed the filesystem's max filename length and fail with ENAMETOOLONG, silently aborting that one record. Keep 'component' to 1-2 short paths or a short label; put the full list of touched files in the correction/previous_assumption text instead.",
      "confidence": "probable"
    }
  ]
}