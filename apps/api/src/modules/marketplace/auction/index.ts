// ponytail: intentionally empty — module boundary scaffolded in Stage 5.
// Time-bound competitive bidding with explicit state transitions and a
// persisted, auditable result (the legacy auction never closed explicitly).
export {};
