// ponytail: intentionally empty — module boundary scaffolded in Stage 5.
// Requirement broadcast -> competing vendor responses -> comparison ->
// selection, with an explicit status transition on close (never delete-to-close).
export {};
