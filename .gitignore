node_modules/
dist/
build/
.next/
.turbo/
coverage/
*.log
.env
.env.local
.DS_Store
*.tsbuildinfo

# Claude Code session-local state, not project config (.claude/settings.json,
# once it exists, is intentionally NOT ignored — that's shared team config).
.claude/*.lock

# Legacy PHP app is reference material only — not part of this codebase.
# It has its own git history; excluded here to avoid nested-repo confusion.
public_html/

# Reference documents (architecture contract, original vision doc)
*.docx

# Approved Claude Design canvas export (frontend-revamp reference material
# only, not part of this codebase) — same treatment as public_html/ above.
Form setup and integration questions/

# matha: rebuilt from git history on read — not worth merge-conflicting over.
# (boundaries.json/ownership.json/shape.json under cortex/ are constant, stay committed)
.matha/cortex/analysis.json
.matha/cortex/stability.json
.matha/cortex/co-changes.json
# Convenience printout with an absolute, machine-local path — not portable.
# The real, portable, team-shared config is the root .mcp.json.
.matha/mcp-config.json

fleeteip
fleeteip/*
formats